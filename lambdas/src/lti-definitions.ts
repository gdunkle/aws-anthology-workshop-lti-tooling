export interface PlatformConfig {
    PK?: string,
    auth_token_url: string;
    auth_login_url: string;
    client_id: string;
    lti_deployment_id: string;
    iss: string;
    key_set_url: string;
}